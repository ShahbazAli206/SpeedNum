# Video / screen recordings are tracked via Git LFS (see .gitattributes),
# since they exceed GitHub's 100 MB per-file limit for regular objects.

# OS / editor cruft
Thumbs.db
Desktop.ini
.DS_Store
.vscode/
.idea/
