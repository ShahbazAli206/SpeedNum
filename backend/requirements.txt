fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0
sqlalchemy[asyncio]>=2.0.30,<2.1
asyncpg>=0.29,<0.31
pydantic[email]>=2.7,<3.0
pydantic-settings>=2.3,<3.0
PyJWT[crypto]>=2.8,<3.0
argon2-cffi>=23.1,<24.0
python-multipart>=0.0.9
httpx>=0.27,<1.0
openpyxl>=3.1,<4.0
boto3>=1.34,<2.0
bleach>=6.1,<7.0
# Video calling: mints/verifies short-lived LiveKit room-access tokens
# server-side (services/livekit_tokens.py). The self-hosted LiveKit SFU
# itself runs as a separate Docker service, not a Python dependency — see
# deploy/docker-compose.yml's `livekit` service.
livekit-api>=1.2,<2.0
